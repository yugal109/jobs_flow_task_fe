const ROOTS = {
  BASE: "",
};

export const paths = {
  // AUTH
  auth: {
    signIn: `${ROOTS.BASE}/sign-in`,
    signUp: `${ROOTS.BASE}/sign-up`,
  },
  base: {
    home: `${ROOTS.BASE}/`,
  },
};
